" Set the default width of a window.
set winwidth=84

" Set the default height of the current window to be as large as possible and
" a minimum height for the other windows. Before setting a large height for
" the current window it needs to be small, otherwise the minimum height will
" fail.
set winheight=5

" Set both the minimum and maximum height after the workaround has been
" set.
set winminheight=5
set winheight=999
